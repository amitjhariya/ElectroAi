import { HOST } from "./config";

export const BASE_URL =  `${HOST}/api/v1`;

//chat
export const CHAT_URL = `${BASE_URL}/chat`;

//docs
export const DOCUMENTS_URL = `${BASE_URL}/docs`;

//ai
export const CONFIG_URL = `${BASE_URL}/config`;

//models
export const MODEL_URL = `${BASE_URL}/models`;

